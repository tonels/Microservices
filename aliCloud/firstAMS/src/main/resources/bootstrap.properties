spring.application.name=firstAMS

# sentinel
spring.cloud.sentinel.transport.dashboard=localhost:8888

# nacos
nacos=localhost:8848
spring.cloud.nacos.discovery.server-addr=${nacos}
spring.cloud.nacos.config.server-addr=${nacos}

